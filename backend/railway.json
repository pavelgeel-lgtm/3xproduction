{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd /app && cd backend && npm install && cd ../frontend && npm install --include=dev && npm run build"
  },
  "deploy": {
    "startCommand": "cd /app/backend && node src/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}